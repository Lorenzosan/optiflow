#pragma once

#include <cstddef>

namespace optiflow {

/**
 * @brief Index type used for reservoir grid states.
 */
using StateIndex = std::size_t;

/**
 * @brief Index type used for time steps.
 */
using TimeIndex = std::size_t;

/**
 * @brief Index type used for action candidates.
 */
using ActionIndex = std::size_t;

} // namespace optiflow
